{
  "name": "db",
  "targets": {
    "prisma:generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx prisma db pull --schema=libs/db/prisma/schema.prisma && npx prisma generate --schema=libs/db/prisma/schema.prisma"
      }
    }
  },
  "tags": ["type:lib", "scope:db"]
}
